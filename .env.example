# Proposed configuration only. Application code does not exist yet.
# Copy to .env locally; never commit real values.
APP_ENV=development
PUBLIC_APP_URL=http://localhost:8000
GEMINI_API_KEY=
DATABRICKS_HOST=
DATABRICKS_WAREHOUSE_ID=
DATABRICKS_CATALOG=
DATABRICKS_SCHEMA=hokieaccess
# Choose the supported auth method after workspace access is verified.
# For deployment prefer a scoped service principal where supported.
DATABRICKS_CLIENT_ID=
DATABRICKS_CLIENT_SECRET=
# Optional limited-duration PAT if the workspace permits and requires it.
DATABRICKS_TOKEN=

# Optional tracks, disabled until the core application works.
ELEVENLABS_API_KEY=
GODADDY_API_KEY=
GODADDY_API_SECRET=
ANS_AGENT_HOST=
