# Paste this into the next session

Continue implementing HokieCare in `C:\Users\sam\OneDrive\Documents\ChatGPT\VTHacks`. Our deployed frontend is https://vthacks-2026-production.up.railway.app and the repo is https://github.com/sam044/vthacks-2026. I want a working scheduling product, not just links to other websites. Begin implementing the plan below; don't stop after giving me another plan.

First read AGENTS.md, `docs/BOOKING_AGENT_NEXT_SESSION_DESIGN.md` (the detailed specification), `docs/HANDOFF.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/DATABRICKS_SETUP.md`, and `docs/SCHIFFERT_FEASIBILITY.md`. Inspect Git status and preserve unrelated changes. The last app baseline when this handoff was written was `f4019608079171b2e0e86349a16cf8b857a5336a`; recheck current state rather than resetting to it. Preserve the Requirements block in PROJECT_PLAN.md. Continue the existing React/FastAPI/Railway app.

My two priorities:

1. **Monthly calendars and shared availability.** For each scheduled service across Schiffert, Cook, TimelyCare, Carilion, and Hokie Wellness, show the current month/day, navigation between months, and a selected-day timetable of free/busy times. Other students should see anonymous busy states, never each other's identities or appointment details. Respect service/resource capacity, hours, duration, holidays and college breaks. Use the correct VT calendar. Exclude on-demand TalkNow from reservable calendars. Academic breaks may pause explicitly labeled campus demo schedules; they are not evidence of actual provider closures. Unknown real availability must stay unknown.

2. **A chatbot that executes booking.** Improve Ask HokieCare from its current one-turn navigation router into a conversational booking agent. It should clarify service/date/time preferences, fetch actual inventory, present a review, book through the same backend as the calendar, and update the appointment record and other viewers. For real providers, users enter their own login and Duo and complete required clinical screening; the agent handles the supported scheduling steps. A provider-confirmed appointment must have real confirmation evidence. Never fabricate a booking or treat a clicked slot as confirmation.

Appointments must persist in Databricks. Start by verifying **Lakebase Postgres** availability and actual hosted read/write/transaction behavior, using the already selected Databricks profile `sam`. Follow the core/Lakebase skills and resolve any required database-project selection without reasking me to choose the same workspace. Keep the existing SQLite demo adapter as a fallback while independent work proceeds, but explain any gap rather than pretending SQLite is Databricks. Use atomic conflict protection, stable idempotency keys, owner checks, and a durable outbox with scoped SSE updates/polling fallback. Keep real private appointment data out of public analytics.

Current state: Cook has a functional synthetic SQLite booking flow; the other providers are not fully connected. The optional Schiffert Chrome/Edge companion was built, and an authenticated browser test reached real availability after I completed login/screening. The installed-extension flow and final booking/confirmation are still unverified. **Open VT portal was fixed to work without the extension; preserve that.** Don't confuse Healthy Hokies (Schiffert's portal) with Hokie Wellness (separate consultations).

Use one Databricks orchestrator with validated tools and deterministic provider adapters initially. The current model is `databricks-qwen3-next-80b-a3b-instruct`. Adding Gemini or multiple agents is optional only if a measured capability gap justifies it; more models do not provide provider access. Don't collect credentials, raw health narratives, medical screenshots or other students' records. Don't make fake real appointments for testing. Pause for a specific real-booking review or required human input, and handle unknown submission outcomes without blindly retrying.

Deliver in stages: (A) storage/companion feasibility, (B) shared demo calendar and Databricks persistence, (C) actual conversational demo booking, (D) verified Schiffert completion and private confirmation sync, (E) other proven adapters. Work independently on unblocked pieces while any user action is pending. Verify concurrency with two separate users, persistence/restarts, cross-user calendar updates, date/closure rules, retries and access isolation. Commit/push validated work, deploy through the existing workflow, and report exactly what works versus what still needs provider access. Keep the existing health-intelligence/HCP features working and preserve repo visibility.

Ask only for information or actions you truly need. Make routine implementation decisions yourself. Use the detailed design document for schemas, APIs/tool contracts, provider states, failure recovery, sources, and acceptance tests.
