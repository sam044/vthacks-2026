# Backend secrets only. Never use NEXT_PUBLIC_ for any of these settings.
# Provision a random 32-byte key per version in your backend secret manager.
# Values below are deliberately invalid placeholders, not working credentials.
USER_DATA_HASH_ACTIVE_KEY_ID=k1
USER_DATA_HASH_KEYS_JSON={"k1":"REPLACE_WITH_32_RANDOM_BYTES_IN_BASE64"}
USER_DATA_HASH_NAMESPACE=hokiecare.user-data
